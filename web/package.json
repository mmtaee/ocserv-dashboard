{
  "name": "@ocserv-dashboard/web",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "admin",
    "customer",
    "shared"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "vue": "^3.5.41",
    "vue-i18n": "^11.4.10",
    "vuetify": "^3.13.2"
  }
}
